<!-- <div class="img" -->

<p id="notice"><%= notice %></p>


<h3><%= @result.name %></h3>
<!-- <p> -->

<div class="background" style="background-image: url(<%=@result.image_url%>);">

  <!-- <% image_tag(@result.image_url, class: "background-image") %> <!  size: "300", -->

  <p>
  <strong>Rating:</strong> <%= @result.rating %> out of 5, based on <%= @result.review_count %> reviews.
  <p>
  <strong>Hot take:</strong> <%= @result.snippet_text %>
  <p>
  <strong>Phone:</strong> <%= @result.display_phone %>
  <p>
  <strong>Address:</strong> <%= @result.location.display_address.join(', ') %>
  <p>
  <!-- Latitude: <% @result.location.coordinate.latitude %> -->
  <p>
  <!-- Longitude: <% @result.location.coordinate.longitude %> -->
  <p>
  <!-- Distance from you: <% @result.distance %> miles. -->
  <p>
  <!-- Business yelp id: <% @result.id %> -->

<p>
  <a href="<%= @result.url %>" target="_blank"><strong>Details</strong></a> |
  <a href="https://www.google.com/maps/dir/My+location/<%= @result.location.display_address.join(',+') %>" target="_blank"><strong>Take me there!</strong></a>
<p>
  <%= link_to fa_icon('arrow-left', text: 'Back'), bored_path(params[:id].to_i - 1), class: 'btn btn-sm btn-default' %>
  |
  <%= link_to fa_icon('arrow-right', text: 'Next'), bored_path(params[:id].to_i + 1), class: 'btn btn-sm btn-default' %> <!-- need to add a safety to prevent +6 -->
<p>

 <!-- @bored_result.name  is a __ minute walk from you. -->
<!-- Details | Take me there! | Don't suggest this again | Save for later -->
</div>
